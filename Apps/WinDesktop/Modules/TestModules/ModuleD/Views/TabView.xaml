<UserControl x:Class="ModuleD.Views.TabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:LogXtreme.WinDsk.Modules.TestModules.ModuleD.Names"
             xmlns:inf="clr-namespace:LogXtreme.WinDsk.Infrastructure;assembly=LogXtreme.WinDsk.Infrastructure"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal">
            <Button Command="{Binding NavigateCommand}" 
                    CommandParameter="ViewA" 
                    Content="Navigate to ViewA"/>
            <Button Command="{Binding NavigateCommand}"
                    CommandParameter="ViewB"
                    Content="Navigate to ViewB" />
        </StackPanel>
        
        <TabControl prism:RegionManager.RegionName="{x:Static local:RegionNamesModuleD.RegionTabview}" 
                    Grid.Row="1"/>
    </Grid>
</UserControl>
